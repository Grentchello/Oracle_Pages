---
title: Daily — 2026-08-25
date: 2026-08-25
type: daily-journal
tags: [daily-journal]
---

# 2026-08-25

> Wiki bootstrap day. Vault initialized, auto-log method, MkDocs master dashboard, projects layout, GitHub Pages pending PAT scope update.

## Done
- Cloned [oracle_Vault](https://github.com/Grentchello/oracle_Vault) and wired Hermes with fine-grained PAT (Contents: Read+Write)
- Created wiki skeleton: SCHEMA, index, log, ORIENTATION
- Moved repo to `/opt/data/hermes_work/` per Grant's layout rule
- Wrote `_meta/method.md` (operating agreement) and `_meta/daily-journal.md` (format spec)
- Created `auto-log` skill to enforce auto-logging on every session
- Set up **MkDocs Material master dashboard** with phone-friendly theme
- Restructured git repo: `/opt/data/hermes_work/` is now the root, `wiki/` is a subfolder
- Created `projects/` with index + `_template/` for sub-dashboards
- Created `daily/`, `entities/`, `concepts/`, `comparisons/`, `queries/` indexes
- Added custom CSS for phone polish (`assets/extra.css`)
- Wrote GitHub Actions workflow (`.github/workflows/docs.yml`) for auto-deploy
- Verified MkDocs build locally — all pages return 200, mobile viewport OK
- Pushed restructure (commits `be628b9`, `3ce4dff`) — workflow file pending PAT upgrade

## Achievements
- **Master dashboard architecture** ready — single phone-accessible hub for all projects
- Wiki is now a real documentation site, not just a folder of markdown
- Per-project sub-dashboards via simple folder convention
- Auto-deploy pipeline configured (just needs workflow-scope PAT to land)

## Goals
- [ ] Grant regenerates PAT with `workflow` scope (or opts out for manual paste)
- [ ] Enable GitHub Pages on the repo (Source = GitHub Actions)
- [ ] Verify dashboard at https://grentchello.github.io/oracle_Vault/
- [ ] Wait for Grant's first real content / source to ingest

## Notes
- Could not write to `/hermes_work` (root, no sudo) — settled on `/opt/data/hermes_work`
- MkDocs requires config file to NOT be in docs_dir — moved mkdocs.yml to repo root
- README.md renamed to ORIENTATION.md to avoid MkDocs auto-detecting it as homepage
- The other Hermes agent Grant used writes to a different repo — these are parallel vaults
- **`workflow` PAT scope required** for pushing GitHub Actions YAML (security default)

## Sessions
- [09:30-11:15 UTC] telegram (Grant) — vault bootstrap, layout decision, skill creation
- [11:15-12:30 UTC] telegram (Grant) — MkDocs dashboard build, repo restructure
