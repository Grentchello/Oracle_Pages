---
title: Daily — 2026-08-25
date: 2026-08-25
type: daily-journal
tags: [daily-journal]
---

# 2026-08-25

> Wiki bootstrap day. Vault, auto-log, MkDocs master dashboard live on GitHub Pages. First project: memecoin-trading bot.

## Done
- Cloned [oracle_Vault](https://github.com/Grentchello/oracle_Vault) and wired Hermes with fine-grained PAT (Contents: Read+Write + Workflow)
- Created wiki skeleton: SCHEMA, index, log, ORIENTATION
- Moved repo to `/opt/data/hermes_work/` per Grant's layout rule
- Wrote `_meta/method.md` (operating agreement) and `_meta/daily-journal.md` (format spec)
- Created `auto-log` skill to enforce auto-logging on every session
- Set up **MkDocs Material master dashboard** with phone-friendly theme
- Restructured git repo: `/opt/data/hermes_work/` is now the root, `wiki/` is a subfolder
- Created `projects/` with index + `_template/` for sub-dashboards
- Created `daily/`, `entities/`, `concepts/`, `comparisons/`, `queries/` indexes
- Added custom CSS for phone polish (`assets/extra.css`)
- Created [Oracle_Pages](https://github.com/Grentchello/Oracle_Pages) (public repo) for GitHub Pages deployment
- Built GitHub Actions workflow: clones private `oracle_Vault`, runs MkDocs, deploys to Pages
- Fixed three build failures: setup-python cache, id-token permissions, pip upgrade
- **Dashboard live at https://grentchello.github.io/Oracle_Pages/**
- **Created first project: `memecoin-trading`** — autonomous memecoin trading bot, ground zero
  - Project page at `wiki/projects/memecoin-trading/index.md`
  - Strategy candidates, infra decisions, risk params all laid out
  - Auto-deployed to dashboard (build #4 succeeded)

## Achievements
- **Phone-accessible master dashboard** working end-to-end
- Single hub for all projects — no port juggling
- Content stays private in `oracle_Vault`; only the rendered site is public
- Auto-deploy on every push, plus a 10-minute cron for catching vault changes
- One master URL for everything; per-project sub-dashboards via folder convention
- **First real project scaffolded** with planning, status, and decision-tracker sections

## Goals
- [ ] Decide on chain + strategy for memecoin-trading bot
- [ ] Start a research pass on Solana memecoin tooling (Jupiter, Birdeye, etc.)

## Notes
- Could not write to `/hermes_work` (root, no sudo) — settled on `/opt/data/hermes_work`
- MkDocs requires config file outside `docs_dir` — `mkdocs.yml` at repo root
- README.md renamed to ORIENTATION.md to avoid MkDocs auto-detecting it as homepage
- The other Hermes agent Grant used writes to a different repo — these are parallel vaults
- Private vault content + public Pages output = clean separation
- Workflow now triggers on push (instant) + every 10 minutes (cron for vault-only changes)

## Sessions
- [09:30-11:15 UTC] telegram (Grant) — vault bootstrap, layout decision, skill creation
- [11:15-12:30 UTC] telegram (Grant) — MkDocs dashboard build, repo restructure
- [12:30-13:00 UTC] telegram (Grant) — Oracle_Pages public repo + GitHub Pages deploy
- [13:00-13:30 UTC] telegram (Grant) — first project: memecoin-trading bot
