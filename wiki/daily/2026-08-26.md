---
title: Daily — 2026-08-26
date: 2026-08-26
type: daily-journal
tags: [daily-journal]
---

# 2026-08-26

> Big bot progress day. v1 → v4 transition. First real losses. Multiple bug fixes.

## Done
- **Bot v1 (Phase 1):** live with DexScreener + pump.fun top-runners feed
- **Bot v2 (Phase 2):** added position manager, entry signals (top-runners + liquidity + volume), exit rules (TP/SL/time-stop), trade ledger. 6 initial buys ($unc, $Toad, $Queefcoin, $Panana, $gOOn, $LEGEND)
- **Live prices via WebSocket:** added `pumpportal.fun` for sub-second price updates on phone dashboard
- **Bot v3:** LLM-decided entries/exits with "attention-first" prompt (focus on new launches, not top-runners)
- **Bot v4:** attention-first + held-position price fetching + liquidity-aware entry/exit + LLM auto-stale detection
- **Dashboard wiring:** GitHub Pages rebuild cron (every 2 min), Pages workflow --strict removed (was breaking builds)
- **Held prices bug fixes:**
  - Hard stop block now uses `held_prices` directly (was using `tokens` fresh list → missed all aged positions)
  - Stale exit block same fix
  - Take-profit tiers (+30%/+100%/+300%) auto-fire
- **Liquidity gate:** bot rejects entries where pool liquidity < 5x position size (prevents buying $1k pools with $100 positions that can't be exited)
- **Exit cap:** if position > 30% of pool, sells are capped to avoid -90% slippage
- **Tasks feature:** private password-protected todo list on master dashboard. Default password "oracle" → user set to `aW0^n7qZ^S`. Password hash + plain stored in long-term memory
- **caveman mode installed:** `npm install -g @caveman-ai/cli && caveman setup --install`. Skill content added to `_meta/method.md` so future sessions know to use it
- **Project page updated** with v4 strategy + liquidity gates + TP tiers

## Achievements
- Bot went from rule-based to LLM-decided (v1 → v4)
- Held position price bug fixed multiple times (held tokens not in fresh-launches list)
- Dashboard renders prices for all held positions (not just fresh ones)
- Tasks feature live with password gate

## Goals
- [x] Decide on chain + strategy for memecoin-trading bot — Solana + LLM-decided
- [x] Start a research pass on Solana memecoin tooling — done
- [x] Wait for first closed trade to evaluate win rate — 7 trades, 1W/6L, learning from post-mortems

## Notes
- **Hard stop bug was the biggest cost driver** — top 10 losses were all -77% to -99.9% because hard stop check looked up tokens in fresh-launches list, but held tokens age out of that list, so check `continue`'d without firing. Now uses `held_prices` directly.
- **Win rate 46% looks fine but losses are bigger** because:
  - Losers ride to -50% hard cap (or worse, the bug above) → -50% per loss
  - Winners sold early at +30%/+100% via partial takes → ~+30-50% per win
  - Need either: tighter losers (cap at -25%?) OR let winners run longer
- Daily PnL cap (-0.4 SOL) hit multiple times — bot halts on bad days
- Bot scans every 60s now (was 5 min)
- Pages CDN caches state.json for 2-5 min so dashboard lags real bot state

## Sessions
- [Bot ran 60s ticks throughout day — 512 trades total, 235W/277L, -1.14 SOL realized]
- [Major code rewrites: bot.py went from 30KB to 50KB]
